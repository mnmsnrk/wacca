<%= render "layouts/shared/header" %>

<%# 商品の概要 %>
<div class="item-show">
  <div class="item-box">
    <div class='item-img-content'>
      <%= image_tag @resume.image, class: 'item-box-img' %>  
    </div>

    <div class="item-price-box">
    </div>

          <%# <% if user_signed_in? && current_user.id == @item.user_id && @item.purchase === nil%>
              <%# <%= link_to '商品の編集', edit_item_path(@item.id), method: :get, class: "item-red-btn" %>
                <%# <p class='or-text'>or</p>
              <%= link_to '削除', item_path(@item.id), method: :delete, class:'item-destroy' %>
            <%# <%elsif @item.purchase === nil %>
                <%# <%= link_to '購入画面に進む',  item_purchases_path(@item.id) ,class:"item-red-btn"%>
                <%# <%else%>
          <%# <% end %>  

    <div class="item-explain-box">
      <span><%= safe_join(@resume.introduction.split("\n"),tag(:br))%></span> 
    </div>
    <table class="detail-table">
      <tbody>
        <tr>
          <th class="detail-item">スピーカー</th>
           <td class="detail-value"><%= @speaker.nickname %></td> 
        </tr>
        <tr>
          <th class="detail-item">カテゴリー</th>
          <td class="detail-value"><%= @resume.category.name %></td>
        </tr>
        <tr>
          <th class="detail-item">主な活動地域</th>
          <td class="detail-value"><%= @resume.prefecture.name %></td>
        </tr>
        <tr>
          <th class="detail-item">講演金額（1h/）</th>
           <td class="detail-value"> ¥<%= @resume.price %></td> 
        </tr>
      </tbody>
    </table>
    <%# <div class="option">
      <div class="favorite-btn">
        <%= image_tag "star.png" ,class:"favorite-star-icon" ,width:"20",height:"20"%>
        <%# <span>お気に入り 0</span>
      </div>
      <div class="report-btn">
        <%= image_tag "flag.png" ,class:"report-flag-icon" ,width:"20",height:"20"%>
        <%# <span>不適切な商品の通報</span>
      </div>
    </div> %> 
   </div> 
  <%# /商品の概要 %>

</div>


